# Doodle Noodle

Astro marketing site for Doodle Noodle Studio.

## Analytics

The Google Analytics tag is defined once in `src/layouts/BaseLayout.astro`.

Set `PUBLIC_GA_ID` in Vercel to rotate the measurement ID without changing code. If it is not set, the layout falls back to the current production ID.
